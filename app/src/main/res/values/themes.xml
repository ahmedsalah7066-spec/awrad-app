<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Awrad" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primaryGreen</item>
        <item name="android:windowBackground">@color/backgroundBeige</item>
        <item name="android:statusBarColor">@color/backgroundBeige</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Awrad" parent="Base.Theme.Awrad" />
</resources>